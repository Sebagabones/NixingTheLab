[project]
name = "python_flake_template"  # TODO:
version = "0.0.1"               # TODO:
description = "A python flake template" # TODO:
authors = [
    { name = "Sebgabones", email = "133339614+Sebagabones@users.noreply.github.com" } # TODO
]
requires-python = ">=3.14"
dependencies = [
]

[project.scripts]
python_flake_template = "python_flake_template:main" # TODO:

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel.hooks.mypyc]
dependencies = ["hatch-mypyc"]
