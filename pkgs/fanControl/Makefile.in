prefix = @prefix@
exec_prefix = @exec_prefix@
bindir = @bindir@

tempChecker: tempChecker.c
	$(CC) -Wall -O3 -o tempChecker tempChecker.c

clean:
	rm -f tempChecker

install: tempChecker
	install -d $(bindir)
	install -t $(bindir) tempChecker
